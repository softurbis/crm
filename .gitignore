node_modules
dist
dist-panel
dist-publico
.env
.env.local
.env.r2
.env.r2.*
.env.migracion
*.log
.DS_Store

# agente whatsapp: secretos y sesion NUNCA al repo
agente/.env
agente/.env.save*
agente/auth/
agente/auth_s/
agente/msgstore.json
agente/node_modules/

# agentes Python (marketing / negocio): CODIGO + DATOS sensibles NUNCA al repo publico.
# Van al droplet por canal privado. El panel /marketing vive en src/ y ese SI se sube.
agente marketing urbis/
agente-negocio/
